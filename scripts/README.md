# PDF generation

From the repository root:

```powershell
py -m pip install -r requirements.txt
py scripts\generate_case_study.py
```

The generated portfolio PDF is written to:

`deliverables/HORMONA-Case-Study-Assa-Traore.pdf`
